## Qwen smoke test

This repo has a very small local test script for `Qwen/Qwen3.5-0.8B`.

Run it with:

```bash
python main.py
```

The script loads the model, sends one user prompt, and prints the generated answer.
